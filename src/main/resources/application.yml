spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/Documents/dev/tools/h2/db-files/jpashop
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
#        format_sql: true

#logging.level:
#    org.hibernate.SQL: debug
#    org.hibernate.orm.jdbc.bind: trace